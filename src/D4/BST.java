package D4;

import com.sun.source.tree.BinaryTree;
import java.util.ArrayList;
import java.util.List;

public class BST_to_list {

    public void main(String[] args){
        // make a list of doubles
        // call the function to convert the list into a balanced BinaryTree
    }

    private static BinaryTree BST(ArrayList<Double>[] a) {
        // make two list from the split function, take both the lists
        // get the mid point of the, assuming they are both ordered
        // insert the mid point in the BinaryTree
        // do this until the arrays are of length 1
    }

    private static List[] split(ArrayList<Double>[] list){
       // Take the list and split it into to part, then return them
    }
}

